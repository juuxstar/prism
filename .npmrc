; npm 12 refuses git dependencies by default, which blocks `npm install` outright: the lint config is a git
; spec. `root` allows the ones this package.json names and still refuses any that arrive through a dependency.
allow-git=root
